MESSAGE TO PROFESSOR: Before running our code make sure to pip install because these 
are third party for FLASK

Need to run "pip install -U flask-cors" In Terminal
Need to run "pip install -U flask-mail" In Terminal